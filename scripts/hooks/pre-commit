#!/usr/bin/env bash
# Sync inference.yml model choices whenever config/models.py is staged.
if git diff --cached --name-only | grep -q 'config/models.py'; then
  python3 scripts/sync_workflow_choices.py
  git add .github/workflows/inference.yml
fi
